<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionBarSize">@dimen/custom_title_bar_height</item><!--设置ActionBar的高度-->
        <item name="actionBarStyle">@style/AppThemeStyle</item>
    </style>

    <style name="AppThemeStyle" parent="@style/Widget.AppCompat.ActionBar">
        <item name="background">@color/black</item>
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetEnd">0dp</item>
        <item name="android:paddingLeft">0dp</item>
        <item name="android:paddingRight">0dp</item>
        <item name="elevation">0dp</item>
    </style>

    <style name="ThemeStart" parent="android:Theme">
        <item name="android:windowBackground">@drawable/common_loading</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="CustomTitleBarTheme" parent="android:Theme.Light">
        <!--定义标题栏的高度-->
        <item name="android:windowTitleSize">@dimen/custom_title_bar_height</item>
        <!--定义标题栏的背景-->
        <item name="android:windowTitleBackgroundStyle">@style/WindowTitleBackground</item>
    </style>

    <style name="WindowTitleBackground">
        <item name="android:background">@color/black</item>
    </style>

    　　<!-- 自定义的ActionBar Style-->
    <style name="CustomActionBarStyle" parent="@android:style/Widget.DeviceDefault.Light.ActionBar">
        <item name="contentInsetEnd">0dp</item><!--右边距为0-->
        <item name="contentInsetStart">0dp</item><!--左边距为0-->
    </style>
    　　<!-- 自定义的Theme-->
    <style name="CustomTheme" parent="@android:style/Theme.DeviceDefault.Light.DarkActionBar">
        <item name="colorPrimary">@color/main</item>
        <item name="colorPrimaryDark">@color/main</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionBarSize">@dimen/custom_title_bar_height</item><!--设置ActionBar的高度-->
        <item name="android:actionBarStyle">@style/CustomActionBarStyle</item>
        <!--<item name="statusBarColor">@color/main</item>&lt;!&ndash;效果和colorPrimary一样&ndash;&gt;-->
        <!--<item name="windowLightStatusBar">true</item>&lt;!&ndash;设置StatusBar上Icon高亮&ndash;&gt;-->
        <item name="android:windowTranslucentNavigation">false</item>
    </style>
</resources>
